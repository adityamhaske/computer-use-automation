"""Compile a successful discovery trace into a reviewable ``Capability``.

Where "the model discovers" becomes "the artifact is a capability": literals lift to typed
parameters, the winning resolution becomes a semantic descriptor, and outcomes and recovery rules
are scaffolded as drafts for human review.
"""
